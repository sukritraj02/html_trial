export function getRecommendations() {
  return [
    "Use ARIA landmarks to improve navigation for screen readers",
    "Ensure sufficient color contrast for text elements",
    "Add skip navigation links for keyboard users"
  ];
}